apiVersion: v1
kind: Service
metadata:
  name: {{ include "beads-viewer.fullname" . }}
  labels:
    {{- include "beads-viewer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "beads-viewer.selectorLabels" . | nindent 4 }}
